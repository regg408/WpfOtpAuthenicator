<Window x:Class="Main.View.AddOtpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Main.View"
        xmlns:vm="clr-namespace:Main.ViewModel"
        mc:Ignorable="d"
        Title="Add OTP" Height="200" Width="800" Icon="/Assets/lock.ico">
    <Window.DataContext>
        <vm:AddOtpWindowViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <local:SvgIconButton Grid.Column="0" SvgPath="Assets/upload.svg" Command="{Binding UploadImageCommand}" ToolTip="Upload Image" Width="100" Height="100"/>
            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Column="0" Grid.Row="0" Text="Label:" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24" FontFamily="Consolas"/>
                <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Label}" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24" FontFamily="Consolas"/>
                <TextBlock Grid.Column="0" Grid.Row="1" Text="Issuer:" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24" FontFamily="Consolas"/>
                <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Issuer}" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24" FontFamily="Consolas"/>
                <TextBlock Grid.Column="0" Grid.Row="2" Text="Secret:" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24" FontFamily="Consolas"/>
                <TextBlock Grid.Column="1" Grid.Row="2" Text="{Binding Secret}" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24" FontFamily="Consolas"/>
            </Grid>
        </Grid>

        <Button Grid.Row="1" Content="Confirm" Height="50" Width="100" IsEnabled="{Binding CanConfirm}" Click="OnConfirm" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Window>
