<Window x:Class="Main.View.AddOtpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Main.View"
        xmlns:vm="clr-namespace:Main.ViewModel"
        mc:Ignorable="d"
        Title="AddOtpWindow" Height="200" Width="800">
    <Window.DataContext>
        <vm:AddOtpWindowViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="75"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Upload Image" Command="{Binding UploadImageCommand}"/>
            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Column="0" Grid.Row="0" Text="Issuer:" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" FontFamily="Consolas" FontWeight="Bold"/>
                <TextBlock Grid.Column="0" Grid.Row="1" Text="Secret:" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" FontFamily="Consolas" FontWeight="Bold"/>
                <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Issuer}" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24" FontFamily="Consolas" FontWeight="Bold"/>
                <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Secret}" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24" FontFamily="Consolas" FontWeight="Bold"/>
            </Grid>
        </Grid>

        <Button Grid.Row="1" Content="Confirm" Height="50" Width="100" IsEnabled="{Binding CanConfirm}" Click="OnConfirm" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Window>
