<UserControl x:Class="Main.View.OtpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Main.View"
             xmlns:vm="clr-namespace:Main.ViewModel"
             mc:Ignorable="d" Width="250" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">

    <Grid Margin="10, 10, 10, 10">
        <StackPanel Orientation="Vertical">
            <TextBlock Text="{Binding Issuer}" TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Left"/>
            <TextBlock Text="{Binding OtpCode}" TextWrapping="Wrap" VerticalAlignment="Center" Margin="0" FontSize="36" HorizontalAlignment="Left"/>
            <TextBlock Text="{Binding TimeLeft}" TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Left"/>
        </StackPanel>
    </Grid>
</UserControl>
